# AI議事録ツール — 作業ルール

## 本番反映

依頼された作業が終わったら、**その都度の確認を取らずに `main` へマージ・プッシュしてよい**。
`main` へのプッシュはそのまま本番公開（Netlifyの自動ビルド）になる想定（Netlifyサイトの用意ができ次第）。

条件と守ること:

- `npx tsc --noEmit` と `npm test` が**すべて通った場合のみ** `main` に入れる。
  1つでも落ちたら `main` には入れず、何が落ちたかを報告して止まる。
- テストを飛ばす・無効にする・削除して通すことで条件を満たしたことにはしない。
- 強制プッシュ（`--force`）と履歴の書き換えはしない。
- 作業は指定された作業ブランチで行い、そこへプッシュしてから `main` にマージする。
- 何を本番に入れたかは毎回報告する。
- 依頼された範囲を超える変更は、勝手に足して本番に入れない。

## 外部サービスのAPIキーが要る変更（人の手が要る）

- Azure AI Speech（音声認識）・Anthropic（質問検知・回答案・要約生成）のAPIキー発行、
  Netlifyの環境変数への設定は、いずれも人（本人）が行う。
- キーが未設定の間は、音声認識は**ブラウザ標準の音声認識（Web Speech API）**を暫定で使い、
  実装を止めない。Azureのキーが用意でき次第、こちらから差し替えの依頼を出す。

## 動作確認のしかた

- 型チェック: `npx tsc --noEmit`
- テスト: `npm test`（Vitest）
