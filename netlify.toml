[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"

# SPA（react-router）なので、ファイルが無いパスは index.html に返す。
# /.netlify/functions/* は Netlify 側が先に処理するのでここには来ない。
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
